# Moved

Python AMC fetchers now live in **`scrapers/python/`**.

This `reference/python-fetchers/` tree is retained temporarily as a mirror during cleanup; prefer `scrapers/python/` for all new work.
